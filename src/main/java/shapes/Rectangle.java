package shapes;

public class Rectangle extends Shape {
    public Rectangle(Point startPoint, Point endPoint, int fillColor, int borderColor, int borderSize) {
        super(startPoint, endPoint, fillColor, borderColor, borderSize);
    }

    @Override
    public String toString() {
        return super.toString();
    }
}
